<Window x:Class="AppUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <!-- Barra de navegación -->
        <Grid Margin="0,0,0,54">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" MinWidth="150"/>
                <ColumnDefinition Width="67"/>
                <ColumnDefinition Width="83"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding CurrentStudentId, RelativeSource={RelativeSource AncestorType=Window}}" VerticalAlignment="Center" Grid.Column="2" Margin="5,0"/>
            <Button Content="Anterior" Click="PreviousButton_Click" Margin="17,14,0,0" Grid.Column="3" HorizontalAlignment="Left" Width="56" Height="33" VerticalAlignment="Top"/>
            <Button Content="Siguiente" Click="NextButton_Click" Margin="75,14,0,0" HorizontalAlignment="Left" Width="60" Grid.Column="1" Height="33" VerticalAlignment="Top"/>
            <TextBox x:Name="StudentNumberTextBox" Grid.Row="0" Grid.Column="2" Margin="10,14,13,0" RenderTransformOrigin="-0.114,4.66" Height="33" VerticalAlignment="Top"/>
        </Grid>

        <!-- Detalles del estudiante -->
        <Grid Margin="20,50,20,98">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="253*" />
                <ColumnDefinition Width="507*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto" MinHeight="107"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label Content="Nombre:" Grid.Row="0" Margin="167,10,0,5" HorizontalAlignment="Left" Width="56"/>
            <TextBox x:Name="NameTextBox" Grid.Row="0" Grid.Column="1" Margin="5,10,5,10"/>
            <Label Content="Edad:" Grid.Row="1" Margin="167,5,0,5" HorizontalAlignment="Left" Width="62"/>
            <TextBox x:Name="AgeTextBox" Grid.Row="1" Grid.Column="1" Margin="5,5,5,5"/>
            <Label Content="Descripción:" Grid.Row="2" Margin="167,5,0,5" RenderTransformOrigin="-0.419,0.621" HorizontalAlignment="Left" Width="76"/>
            <TextBox x:Name="DescriptionTextBox" Grid.Row="2" Grid.Column="1" Margin="5,5,5,5"
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
            <Button Content="Eliminar Estudiante" Click="DeleteStudentButton_Click" HorizontalAlignment="Right" Margin="0,18,275,0" Width="105" Grid.Column="1" Grid.Row="3" Height="56" VerticalAlignment="Top"/>
        </Grid>

        <!-- Botón para abrir ventana de agregar estudiante -->
        <Button Content="Añadir Estudiante" Click="AddStudentButton_Click" HorizontalAlignment="Right" Margin="0,254,421,0" Height="53" VerticalAlignment="Top"/>
    </Grid>
</Window>
